import React from 'react';
import AppIcon from './images/app-icon.jpg';

const NavBar = () => (
    <nav>
        <img src={AppIcon} alt="something went wrong" />
        <h3>Meme Generator</h3>
    </nav>
);

export default NavBar
